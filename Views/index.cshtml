<h1>CD Repository</h1>

@if (Model.Count == 0)
{
  <p>There are no CDs in the database</p>
}
else
{
  <ul>
    @foreach (var cd in Model)
    {
      <li><a href="/CD/@cd.GetID()">@cd.GetArtist()</a></li>
    }
  </ul>
}
